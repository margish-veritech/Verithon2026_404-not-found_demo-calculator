# SciCalc demo API (server/index.js)
PORT=4100

# NOTE: In this intentionally vulnerable demo the JWT secret and admin password
# are HARDCODED in server/index.js on purpose (that is one of the findings).
# A remediated version would read them from here instead:
# JWT_SECRET=
# ADMIN_PASSWORD=
